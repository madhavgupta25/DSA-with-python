def longest_substring(s:str)->int:
    start = 0
    end = len(s)
    for i in range(len(s)):
        for j in range(len(s)):
            if is_palindrome(s)
    
s = str(input())
print(longest_substring(s))